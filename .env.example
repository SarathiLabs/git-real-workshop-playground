VITE_GITHUB_TOKEN=
VITE_GITHUB_OWNER=SarathiLabs
VITE_GITHUB_REPO=git-real-workshop-playground
VITE_USE_MOCK_GITHUB=false
# Leave at 0 for on-demand only (header Refresh button).
# Set e.g. 30000 to poll every 30s — burns rate limit fast in a room.
VITE_REFRESH_INTERVAL_MS=0
